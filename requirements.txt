# AI Assistant Dependencies
numpy>=1.20.0
sounddevice>=0.4.0
faster-whisper>=0.9.0
pynput>=1.7.0
anthropic>=0.40.0
elevenlabs>=1.0.0
spotipy>=2.23.0
requests>=2.28.0
# Optional: only needed for llm_backend="lmstudio" (LM Studio's
# OpenAI-compatible endpoint). llm_backend="lmstudio_api" talks to LM
# Studio's own /api/v1/chat schema over `requests` and doesn't need this.
openai>=1.0.0
# Local RAG knowledge base (ingest.py / rag.py)
sentence-transformers>=2.2.0
chromadb>=0.4.0
pypdf>=4.0.0
